# Playground

Live demo of every component in light / dark / system themes.

```bash
cd examples/playground
npm install
npm run dev
```

The Vite dev server aliases `beautiful-banner-ads` to the parent repo's source — edits to `../../src` hot-reload in the browser.
